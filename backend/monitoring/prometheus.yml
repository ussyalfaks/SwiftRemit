# Prometheus configuration for SwiftRemit Verification Service

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "swiftremit"
    static_configs:
      - targets: ["localhost:3000"]
    metrics_path: "/metrics"
    scrape_interval: 10s
